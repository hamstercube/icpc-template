

### [P1962 斐波那契数列](https://www.luogu.com.cn/problem/P1962 "P1962 斐波那契数列")

考虑构造这样的矩阵：$\begin{bmatrix}  1&1 \\  1&0  \end{bmatrix}$ $\times $ $\begin{bmatrix}  a_n \\  a_{n-1}  \end{bmatrix}$ = $\begin{bmatrix}  a_{n+1} \\  a_{n}  \end{bmatrix}$ 。

怎么想到这个构造的呢？我觉得可以根据递推公式，观察第n项是由前面哪些项得来的，根据矩阵运算法则反推即可 。

利用矩阵快速幂的代码计算出其迭代n-1次后的新矩阵，这样第1行第1列的元素就是$a_n$了(第1行第2列的元素就是$a_{n-1}$)

```c++
vvi mul(vvi a, vvi b, int mod){
	int n = a.size(), m = a[0].size(), k = b[0].size();
	
	vvi c(n, vector<int>(k, 0));
	
	for(int i = 0; i < n; i ++)	
		for(int j = 0; j < k; j ++)
			for(int u = 0; u < m; u ++)
				c[i][j] = (c[i][j] + a[i][u] * b[u][j]) %mod;
				
	return c;
}
vvi qpow(vvi a, int b, int mod){
	int n = (int)a.size();
	vvi c(n, vector<int> (n, 0));
	for(int i = 0; i < n; i++) c[i][i] = 1;//构造单位矩阵
	while(b){
		if(b&1)c = mul(c, a, mod);
		a = mul(a, a, mod);
		b>>=1;
	}
	return c;
}
void solve(){
	int n;
	cin>>n;
	vvi a={{1, 1}, {1, 0}};
	vvi b=qpow(a, n - 1, mod);
	cout<<b[0][0]<<endl;
}
```

### [河南萌新联赛2025第（五）场H题](https://ac.nowcoder.com/acm/contest/115567/H "河南萌新联赛2025第（五）场H题")

斐波那契数列前N项和

考虑构造$\begin{bmatrix}  2&0&-1 \\  1&0&0 \\ 0&1&0 \end{bmatrix}$ $\times $ $\begin{bmatrix}  a_{1}+a_{2}+a_{3} \\  a_{1}+a_{2} \\ a_{1} \end{bmatrix}$ = $\begin{bmatrix}  a_{4}+a_{1}+a_{2}+a_{3} \\  a_{1}+a_{2}+a_{3} \\ a_{1}+a_{2} \end{bmatrix}$

考虑构造

┌       ┐       ┌             ┐       ┌                     ┐
│ 2  0 -1 │       │ a₁+a₂+a₃     │       │ a₄+a₁+a₂+a₃       │
│ 1  0  0 │  ×   │ a₁+a₂       │  =   │ a₁+a₂+a₃         │
│ 0  1  0 │       │ a₁           │       │ a₁+a₂             │
└       ┘       └             ┘       └                     ┘



<details>
<summary>点击查看代码</summary>

```
vvi mul(vvi a, vvi b){
    int n = a.size(), m = a[0].size(), k = b[0].size();
    vvi c(n, vector<int> (k, 0));
    for(int i = 0;i < n; i ++)    
        for(int j = 0; j < k; j ++)
            for(int u = 0 ;u < m; u++)
                c[i][j]=((c[i][j] + a[i][u] *b[u][j])%  mod + mod) % mod;
    return c;
}
vvi qpow(vvi a, int b){
    int n = (int)a.size();
    vvi c(n, vector<int>(n, 0));
    for(int i = 0;i < n; i ++)c[i][i] = 1;
    while(b){
        if(b&1)c = mul(c, a);
        a = mul(a, a);
        b >>= 1;
    }
    return c;
}
void solve(){
    cin >> n >> mod;
    
    vvi res = {{4}, {2}, {1}};// 初始项
    
    vvi tmp = {{2,0,-1}, {1,0,0}, {0,1,0}};// 迭代条件
    
    tmp = qpow(tmp, n - 1);
    
    res = mul(tmp, res);//不要写反了！！！
    
    cout<<res[2][0]<<endl;
}

```

