```cpp
vector<ll> prime;
ll vis[1000100];
const ll N = 1e6;

void ou_shi(){
	for(ll i = 2; i <= N; i++){
		if(vis[i] == 0){
			vis[i] = 1;
			prime.push_back(i);
		}
		for(auto it : prime){
			if(it*i > N) break;
			vis[it*i] = 1;
			if(i%it == 0) break;
		}
	}
}

```